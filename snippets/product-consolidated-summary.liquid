{%- comment -%}
  Renders description summary text consolidated from the blank and design metadata attached to a product

  Accepts:
      nothing
  Usage:
  {%- render 'product-consolidated-summary' -%}

  Author:
  FullChestCovers
  
{%- endcomment -%}
{%- comment -%}
The descriptions are displayed in order from most specific (product) to least specific (blank)
{%- endcomment -%}
{%- capture consolidated_summary -%}
  {{ product.description }}
  {{ product.metafields.custom.product_design.value.description.value }}
  {{ product.metafields.custom.product_blank.value.product_description.value }}
{%- endcapture -%}
{%- if consolidated_summary != empty and consolidated_summary != blank -%}
  {{ consolidated_summary }}
{%- endif -%}
 