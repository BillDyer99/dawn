{% comment %}
  Renders the beginning of icon with text list

  Accepts:
  - link_image: {String} The name of the image to display.  The full url is not needed, it will be prepended if not supplied.
      Only an image or an icon can be provided, if both are provided the image will take precedence.
  - link_icon: {String} The name of the icon to display. The full url is not needed, it will be prepended if not supplied.
      Only an image or an icon can be provided, if both are provided the image will take precedence.
  - link_heading: {String} The text to render.  Can be HTML markup.

  Usage:
  {% render 'icon-with-text-list-item', icon: "Shirt", heading: "I'm a shirt" %}

  Author:
  FullChestCovers
  
{% endcomment %}
    <li class="icon-with-text__item">
      {%- if link_image == null -%}
        {%- render 'icon-accordion', icon: link_icon -%}
      {%- else -%}
        <img
          src="{{ link_image | image_url }}"
          {% if link_image.alt != blank %}
            alt="{{ link_image.alt | escape }}"
          {% else %}
            role="presentation"
          {% endif %}
          height="auto"
          width="auto"
          loading="lazy"
        >
      {%- endif -%}
      <span class="h4 inline-richtext">
        {{- link_heading -}}
      </span>
    </li>