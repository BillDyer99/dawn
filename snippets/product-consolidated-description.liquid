{%- comment -%}
  Renders description text consolidated from the blank metadata attached to a product, and the product description.

  Accepts:
      nothing
  Usage:
  {%- render 'product-consolidated-description' -%}

  Author:
  FullChestCovers
  
{%- endcomment -%}
{%- capture consolidated_description -%}
   {{ product.description }}
   {{ product.metafields.custom.product_design.value.product_description.value }}
   {{ product.metafields.custom.product_blank.value.product_description.value }}
{%- endcapture -%}
{%- if consolidated_description != empty and consolidated_description != blank -%}
  {{ consolidated_description }}
{%- endif -%}
 